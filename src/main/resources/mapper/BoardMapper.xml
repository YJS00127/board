<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.board.mapper.BoardMapper">

    <select id="findBoardAll" resultType="BoardDTO">
        SELECT id, title, writer, created_at
        FROM board
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <select id="findBoardByTitle" resultType="BoardDTO">
        SELECT id, title, writer, created_at
        FROM board
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <select id="findBoardById" resultType="BoardDTO">
        SELECT id,
        title, content, writer, created_at, pw
        FROM board
        WHERE id= #{id}
    </select>
    <insert id="insertBoard">
        INSERT INTO board(title, content, writer, pw)
        VALUES(#{title}, #{content}, #{writer}, #{pw})
    </insert>
    <update id="updateBoard">
        UPDATE board
        SET title= #{title},
            content= #{content},
            writer= #{writer}
        WHERE id= #{id}
    </update>
    <delete id="deleteBoard">
        DELETE FROM board
        WHERE id= #{id} AND pw= #{pw}
    </delete>

<!--    페이지네이션-->
    <select id="countBoards" resultType="int">
        SELECT COUNT(*) FROM board
    </select>

    <select id="countBoardsByKeyword" resultType="int">
        SELECT COUNT(*) FROM board
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
    </select>
</mapper>